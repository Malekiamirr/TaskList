// eslint-disable-next-line react/prop-types
function BoardTitle({ title, titleColor, taskColor, taskCount }) {
  return (
    <div className="w-[300px] h-[18px] flex items-center justify-between"></div>
  );
}

export default BoardTitle;
